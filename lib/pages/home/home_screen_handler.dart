class HomeScreenHandler {
  // List<Widget> buildBooksList(List<Map<String, dynamic>> booksList) {
  //   booksList
  //       .shuffle(); //// Shuffle the booksList to display them in random order

  //   return booksList.map((bookData) {
  //     return buildBookWidget(bookData);
  //   }).toList();
  // }
}
